/**
 * Represents the authentication type.
 * Currently only QR is supported, but additional types may be added in the future.
 */

package com.dgu.clubauth.domain.student.enums;

public enum Type {
    QR
}
